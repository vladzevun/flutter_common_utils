name: example
description: Example app demonstrating ulad_flutter_utils package.

publish_to: 'none'

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  ulad_flutter_utils:
    path: ../
  flutter_screenutil: ^5.9.3
  flutter_bloc: ^9.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
